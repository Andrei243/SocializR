@model  AlbumsViewModel
@{
    ViewData["Title"] = "Albums";
    Layout = "~/Views/Shared/GeneralLayout.cshtml";
}

<h1>My Albums</h1>
<div>
    @foreach (var album in Model.Album)
    {
        <div class="albumEdit">
            <h6>@album.Name</h6>
            <a href="@Url.Action("Album","Profile",new {albumId=album.Id })">
                @if (album.CoverPhoto != -1)
                {
                    <img src="@Url.Action("Download", "Photos", new {id=album.CoverPhoto})" height="40" />
                }
                else
                {
                    <img src="~/images/genericAlbumCoverPhoto.jpg" height="40" />
                }
            </a>
            <p>@album.Count Photos</p>
            <a href="/Profile/RemoveAlbum?albumId=@album.Id" class="btn btn-danger btn-sm needConfrmation">Delete this album</a>
        </div>

    }
    @Html.Partial("PartialAddAlbum", Model.AddAlbumModel)
</div>
